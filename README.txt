To load project:
1) make sure pipenv is installed
2) clone repo to a directory
2) run "pipenv install" in that repo
4) in the backend, run "python manage.py makemigrations"
5) in the backend, run "python manage.py migrate"
6) in the backend, run "python manage.py runserver"
